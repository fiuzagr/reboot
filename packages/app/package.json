{
  "name": "app",
  "version": "1.0.0",
  "description": "Main package",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "env BOOT_DEBUG=1 boot serve:webpack web",
    "serve:release": "env BOOT_DEBUG= boot serve:webpack web",
    "build:debug": "env BOOT_DEBUG=1 boot build:webpack web",
    "build:release": "env BOOT_DEBUG= boot build:webpack web",
    "build": "npm run build:debug && npm run build:release"
  }
}
