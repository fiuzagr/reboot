export const urls = {
  api: 'http://api.production'
};

export const environment = 'production';

export const debugMode = false;

export const version = process.env.VERSION;

export const gaId = 'UA-xxxxxxxxx-x';
