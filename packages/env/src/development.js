export const urls = {
  api: 'http://api.development'
};

export const environment = 'development';

export const debugMode = true;

export const version = process.env.VERSION;

export const gaId = 'UA-xxxxxxxxx-x';
