{
  "name": "@reboot/env",
  "version": "0.0.0",
  "description": "Environment package",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build -- -w",
    "build": "env BOOT_LERNA=1 boot build:babel default"
  }
}
